spring.application.name=Projet_PFE

# ============================
# = CONFIGURATION DE LA BASE =
# ============================

# Lien vers la base de donnes MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/stage_db

# Identifiants de connexion
spring.datasource.username=root
spring.datasource.password=Crjuvew@c7

# Driver JDBC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ============================
# = JPA / HIBERNATE ==========
# ============================

# JPA = Java Persistence API (connexion Java ? base de donn�es)
# Creation automatique du schma
spring.jpa.hibernate.ddl-auto=update

# Affiche les requetes SQL dans la console
spring.jpa.show-sql=true

# Formatage lisible des requtes SQL
spring.jpa.properties.hibernate.format_sql=true

#---detecte automatiquement
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ============================
# = CONFIGURATION GwNeRALE ==
# ============================

# Definit le port de l'application (facultatif)
server.port=8080


logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE



# JWT Configuration
app.jwt-secret=YourBase64EncodedSecretKeyThatShouldBeAtLeast256BitsLong
app.jwt-expiration-milliseconds=86400000
